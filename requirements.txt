# st.dialog and st.html both need a recent Streamlit. An older one already on
# the machine will satisfy a loose pin and silently render dialogs inline, so
# this floor is deliberate - upgrade with:  pip install -U -r requirements.txt
streamlit>=1.45
plotly>=5.20
pydantic>=2.6
httpx>=0.27
